<h1>Editing group</h1>

<%= render 'form' %>

<%= link_to 'Show', [@user, @group] %> |
<%= link_to 'Back', user_groups_path(@user) %> |
<%= link_to 'Sign out', signout_path, method: :delete, data: { confirm: 'Are you sure?' } %>
